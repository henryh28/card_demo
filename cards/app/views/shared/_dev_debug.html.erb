<fieldset class="debug">
  <legend>Developer Details</legend>
  system notices: <%= flash[:notice] %><br>
  ---------------------------------------<br>
  hand:
  <% session[:player_hand].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  deck:
  <% session[:player_deck].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  discard:
  <% session[:player_discard].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  ---------------------------------------<br>
  event hand:
  <% session[:event_hand].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  event deck:
  <% session[:event_deck].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  event discard:
  <% session[:event_discard].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  ---------------------------------------<br>
  buy hand:
  <% session[:buy_hand].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  buy deck:
  <% session[:buy_deck].each do |card| %>
    <%= card.id %>,
  <% end %><br>
  =======================================<br>

</fieldset>
